import 'package:flutter/material.dart';

class ReferalInfoModel {
  final String? title;
  final int? count;
  final Color? color;
  final IconData? icon;

  ReferalInfoModel({
    this.icon,
    this.title,
    this.count,
    this.color,
  });
}

